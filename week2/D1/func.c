/*------PI----*/
void matmult_nat(int M, int N, int K, double **A, double **B, double **C) {
	for (int l = 0; l < M*N; l++) {
	temp = 0;
	}
		for (int n = 0; n < N; n++) { 
		temp = temp + (4/((1+pow((i-0.5)/n),2)))
    }
}